# FleetX frontend

This directory contains the working localhost React/Vite application. It is intentionally kept as the source of truth for the FleetX white/orange enterprise dashboard.

```bash
npm install
npm run build
npm run dev
```

The dev server runs at http://localhost:3000 and calls the FastAPI service at http://127.0.0.1:8000.
